import initialState, { FooterState } from "./initialState";

const footerReducer = (state: FooterState = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default footerReducer;
